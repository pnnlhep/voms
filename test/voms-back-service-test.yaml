kind: Service
apiVersion: v1
metadata:
  labels:
    app: voms-backend-test
  name: voms-backend-test
  namespace: voms
spec:
  selector:
    app: voms-backend-test
  ports:
  - protocol: TCP
    port: 3306
