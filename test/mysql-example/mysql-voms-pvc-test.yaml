apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: voms-db-vol-test
  namespace: voms
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: voms-db-backfs-test
